# dot-claude (Project .claude/)

This is the local testing copy. See root CLAUDE.md for project documentation.

Source files are in the project root. This directory is for local testing before global deployment.

## Sync Command
```bash
# From project root:
cp -r commands/* .claude/commands/
cp -r scripts/* .claude/scripts/
cp -r rules/* .claude/rules/
cp -r modules/* .claude/modules/
```
